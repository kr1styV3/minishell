{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/minishell",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "sourceMap": {
                "/build/root": "${workspaceFolder}"
            },
            "stepInTarget": true,
            "logging": { "engineLogging": true }  // Enable engine logging to troubleshoot issues
        }

    ]
}
