{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",                          // Use CodeLLDB type
            "request": "launch",
            "program": "${workspaceFolder}/minishell", // Path to your executable
            "args": [],                                // Command-line arguments (if any)
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",              // Working directory
            "environment": [],
            "externalConsole": false
        }
    ]
}
